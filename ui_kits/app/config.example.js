/* Optional frontend config. Copy to config.js to override API URL.
 * API keys go in the repo root .env file — NOT here (see README). */

window.MEDIFI_CONFIG = {
  // Leave empty to use same host, or http://localhost:3001 when using python http.server
  apiBase: "",
};
