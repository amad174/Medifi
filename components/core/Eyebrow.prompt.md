Tracked uppercase overline label that sits above a heading — the one place ALL CAPS is allowed.

```jsx
<Eyebrow tone="accent">Your plan</Eyebrow>
<h2>What you need to do</h2>
```

Tones: `muted` (default), `brand`, `accent`.
